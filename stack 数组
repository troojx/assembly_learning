栈：
  mov ax,0123H
  push ax

从栈顶到栈底：1000H，1001H....100EH，100FH
01为AH，23为AL ； 01先入栈，放入100FH地址 ； 23后入栈，放入100EH

段寄存器SS，寄存器SP ： 栈顶的段地址存放在SS中，偏移地址存放在SP中
任意时刻，SS:SP指向栈顶元素

push ax 执行步骤:
  1.SP = SP-2, SS:SP指向当前栈顶前面的单元，以此为新的栈顶
  2.将ax的内容送入SS:SP指向的内存单元，此时SS:SP指向新栈顶
push 操作：先读取数，再改变SP

栈顶超界的问题



代码段可以用编号
数据段也可以用编号

一般来说，不在代码段定义数据，而是将数据定义到其他段
例如 在数据段中，arr dw 12,34,56,78 
                arr2 db 'hello world' 
     arr3:db 1,2,3,4,5,6,7,8 这种：地址标号只能在代码段中使用
数组既可以按照下标读取（下标改变1是一个字节），也可以写入
